import React from "react";
import { strings } from "../constants";

const { Home_Page_Text } = strings;

export const Home = () => {
    return (
        <div>
            {Home_Page_Text}
        </div>
    );
};