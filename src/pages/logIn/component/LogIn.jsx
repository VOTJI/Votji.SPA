import React from 'react';
import { strings } from '../constants';

const { LogIn_Page_Text } = strings

export const LogIn = () => {
    return (
        <div>
            {LogIn_Page_Text}
        </div>
    )
}