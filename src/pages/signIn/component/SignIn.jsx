import React from 'react';
import { strings } from '../constants';

const { SignIn_Page_Text } = strings

export const SignIn = () => {
    return (
        <div>
            {SignIn_Page_Text}
        </div>
    )
}